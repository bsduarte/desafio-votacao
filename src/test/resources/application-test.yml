spring:
  datasource:
    url: jdbc:tc:cleisonfmelo/postgres-pg-cron:latest://localhost/testdb
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
